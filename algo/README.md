# Importable algo package, can be used with import "github.com/user_name/module_name/sub_package"
# Must have strong release cycle, because can be reused by others:
# import "github.com/levpa/go-base/algo"